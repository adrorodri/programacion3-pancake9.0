<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.andres.mundial">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".UI.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.LogInActivity" />
        <activity android:name=".UI.SignUpActivity" />
        <activity android:name=".UI.MenuActivity" />
        <activity android:name=".UI.TablaPartidosActivity" />
        <activity android:name=".UI.UsuarioActivity" />
        <activity android:name=".UI.ResultadosActivity" />
        <activity android:name=".UI.PosicionesActivity" />
        <activity android:name=".UI.ApuestasActivity"></activity>
    </application>

</manifest>